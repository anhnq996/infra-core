# ============================================
# GoTalk Web Secret - EXAMPLE (safe to commit)
# ============================================
# Frontend (Next.js) chỉ cần secret cho các biến
# server-side hoặc biến public nhạy cảm.
#
# Hướng dẫn:
#   1. cp secret-web.yaml.example secret-web.yaml
#   2. echo -n "your_value" | base64
#   3. Thay REPLACE_BASE64(...) bằng kết quả
#   4. kubectl apply -f secret-web.yaml
#
# ⚠️  KHÔNG commit secret-web.yaml lên Git!
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: gotalk-web-secret
  namespace: gotalk
type: Opaque
data:
  # ---- Google OAuth Client ID (public nhưng tốt hơn quản lý qua Secret) ----
  # Lấy từ: https://console.cloud.google.com
  # Hiện tại: 66123709162-7hg4aaove68o33la1n0qg29346ibvn83.apps.googleusercontent.com
  # echo -n "your-google-client-id" | base64
  NEXT_PUBLIC_GOOGLE_CLIENT_ID: REPLACE_BASE64(GOOGLE_CLIENT_ID)

  # ---- WebSocket URL ----
  # echo -n "wss://api-gotalk.anhnq.io.vn/ws" | base64
  NEXT_PUBLIC_WS_URL: REPLACE_BASE64(WS_URL)
