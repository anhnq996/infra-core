# ============================================
# Redis Secret - EXAMPLE (safe to commit)
# ============================================
# Hướng dẫn:
#   cp secret.yaml.example secret.yaml
#   echo -n "redis_secret_2026" | base64
#   nano secret.yaml
#   kubectl apply -f secret.yaml
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: infra
type: Opaque
data:
  # echo -n "redis_secret_2026" | base64
  redis-password: REPLACE_BASE64(REDIS_PASSWORD)
